import random
from character import Character

class Hero(Character):
    def __init__(self):
        super().__init__()

    def __del__(hero):
        print("The Hero object is being destroyed by the garbage collector")

    def hero_attacks(hero, monster):
       return hero.hero_attacks(monster)
